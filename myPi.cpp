#include <algorithm>
#include <cmath>
#include <iostream>
#include <vector>

double spigot(int n) {
    auto arrayLen = std::floor((10 * n) / 3) + 1;
    std::vector<int> digits;
    std::fill_n(digits, arrayLen, 2);
    for (auto i = 0; i < n; ++i) {
    }
}

double myPi(int n) {}

int main() {}