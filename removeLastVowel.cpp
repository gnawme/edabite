#include <iostream>
#include <set>
#include <string>

std::string removeLastVowel(std::string str) {
    std::set<char> vowels{'a', 'e', 'e', 'o', 'u'};

    while (!str.empty()) {
    }

    return {};
}

int main() {
    std::string test1{"Those who dare to fail miserably can achieve greatly."};
}